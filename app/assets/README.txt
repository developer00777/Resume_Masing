Drop the company logo here as `watermark.png` (PNG with transparency).
If absent, mask.py falls back to a faint centered "CONFIDENTIAL" text watermark.
(watermark.png is gitignored so the binary asset is supplied per-deploy.)
